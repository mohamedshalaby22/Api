import 'package:flutter/material.dart';

class FavouitePage extends StatelessWidget {
  const FavouitePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
    );
  }
}
